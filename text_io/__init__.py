# text_io package
# Text-specific I/O for the game: turning the current text format into a Board and back.
# These classes are tied to *this* input/output format. Other formats (binary, network,
# a GUI) would live in sibling packages and reuse the same Board/engine core untouched.
