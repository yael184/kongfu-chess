# model package
# The core domain model: pure logical building blocks with no knowledge of rendering,
# input coordinates, pixels, timing, or movement rules. Those concerns live in the rules/,
# realtime/, io/, and view/ layers.
