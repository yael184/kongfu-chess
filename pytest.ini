[pytest]
# Collect only the split test suite under tests/.
testpaths = tests
# Put the repo root on sys.path so tests can import top-level packages (e.g. from model.board import Board).
pythonpath = .
