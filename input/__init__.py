# input package
# The input/command layer: translates user actions (clicks) into GameEngine commands.
# It maps pixels to cells and tracks selection, but decides no chess legality and never
# mutates the board directly.
